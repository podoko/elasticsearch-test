framework:
    secret: '%env(APP_SECRET)%'
    test: true
    http_method_override: false

    # Cache en mémoire pour les tests (évite la création de fichiers cache)
    cache:
        app: cache.adapter.array
